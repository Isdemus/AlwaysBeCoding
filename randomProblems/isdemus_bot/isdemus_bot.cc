#include <iostream>
#include <string>
#include <map>
#include <vector>
#include <thread>
#include "bot.h"
#include "unit.h"

#define boardSize 8;

using namespace std;

void setUp(unsigned int** board) {
  
  
}

int main(int argc, char* argv[]) {
  // Making the board
  unsigned int **board = new unsigned int*();
  setUp(board); 

  

  return 0;
}
